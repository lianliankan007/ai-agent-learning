python语法学习

1.self这python语法中类似Java的this
关键点总结
要点	                说明
必须作为第一个参数	       实例方法的第一个参数必须是 self
调用时不用传	          agent.chat("你好") 自动把 agent 作为 self 传入
区分不同实例	          每个实例有自己的 self，数据互不干扰
访问实例属性	          self.属性名 访问当前实例的数据
调用其他方法	          self.方法名() 调用当前实例的其他方法

2._init__ Python的构造函数
def _(self, name, age):
        self.name = name
        self.age = age


# Day 1 学习总结

## 今日学习目标
（简述今天的学习目标）

## 学习内容总结
（用自己的话总结学到的知识点）

## 核心知识点
- Token 文本的基本单位（英文约 1 token = 0.75 单词，中文约 1 token = 1.5 字）
- Context Window  模型单次能处理的 token 数量（如 Qwen3.5 支持 256K）
- LLM API 调用流程 
  - 我的代码 -> LLM -> response ->我的代码
- Python requests 基本用法
  - ` response = requests.post(url, headers=headers, json=data, timeout=60)
            response.raise_for_status()
            result = response.json()
            return result["choices"][0]["message"]["content"]`
- Temperature：控制输出随机性（0=确定，1=创造性）

## 实践任务完成情况
- [ ] 环境准备 
- [ ] agent_runner.py 大模型的入口 注册了两个agent
- [ ] LLMService 类封装 llm_agent.py定义了agent类

## 遇到的问题
（记录遇到的错误和卡点）

## 解决方案
（如何解决的）

## 今日收获
- 了解了一个汉字居然比英文省toke, 1token = 0.75 英文 =1.5汉字
- 了解了Python的基本用法 

## 未理解的问题